# Pages Directory

This directory contains the main page components for the application. Each file here typically corresponds to a specific route defined in `App.tsx`.

## Current Pages

*   **Home.tsx**: The landing page of the application.
*   **Playground.tsx**: A testing ground for UI components and React concepts (State, Events, etc.).
