# Components Directory

This directory contains reusable UI components that can be used across different pages of the application.

## Current Components

*   **Navigation.tsx**: The top navigation bar allowing users to switch between pages.
